version: "3.8"

services:
  llm-dispatch:
    build: .
    container_name: simaple-llm-dispatch
    ports:
      - "3100:3100"
    volumes:
      - ./:/app
    environment:
      - PORT=3100
      - UPSTREAM_TIMEOUT=15
    restart: unless-stopped
